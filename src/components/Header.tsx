import React from 'react';
import { StyledBox, StyledTitle } from "../App.styled";

export const Header = () => {
  return (
    <StyledBox>
      <StyledTitle>
        Video Browser
      </StyledTitle>
    </StyledBox>
  );
};

